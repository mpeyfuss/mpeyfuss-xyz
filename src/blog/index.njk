---
layout: layout.njk
title: Blog
image: /assets/og/blog.png
---

<style>
    .wrapper {
        width: 100%;
        display: flex;
        flex-direction: column;
        margin-top: 4rem;
        text-align: start;
    }

    .pinned-posts-wrapper {
        display: flex;
        flex-wrap: wrap;
        row-gap: 1rem;
        column-gap: 1rem;
        padding: 0;
        margin: 0;
    }

    .pinned-post-wrapper {
        display: flex;
        flex-direction: column;
        border: 1px solid rgb(63, 63, 70);
        border-radius: 0.5rem;
        list-style: none;
        transition-duration: 150ms;
        transition-timing-function: ease-in-out;
        margin: 0;
        padding: 1rem;
        overflow: hidden;
        background-color: white;
    }

    .pinned-post {
        display: flex;
        flex-direction: column;
        row-gap: 1rem;
        width: 100%;
        transition-duration: 150ms;
        transition-timing-function: ease-in-out;
        color: var(--zinc);
    }

    .pinned-post-wrapper:hover {
        border-color: var(--violet-500);
        background-color: var(--violet-500);
    }

    .posts-wrapper {
        display: flex;
        flex-wrap: wrap;
        row-gap: 1rem;
        column-gap: 1rem;
        padding: 0;
        margin: 0;
    }

    .post-wrapper {
        display: flex;
        flex-direction: column;
        border: 1px solid rgb(63, 63, 70);
        border-radius: 0.5rem;
        list-style: none;
        transition-duration: 150ms;
        transition-timing-function: ease-in-out;
        margin: 0;
        padding: 1rem;
        overflow: hidden;
        background-color: white;
    }

    .post {
        display: flex;
        flex-direction: column;
        row-gap: 1rem;
        width: 100%;
        transition-duration: 150ms;
        transition-timing-function: ease-in-out;
        color: var(--zinc);
    }
</style>

<h1 style="margin-bottom:0.5rem;">Blog</h1>
<h4 class="gradient-text" style="text-align:center;margin-top:0.5rem;">Semi-coherent thoughts I scribbled down in haste
</h4>

<div class="wrapper">
    <h4 style="width:100%;margin-top:0px;">Featured</h4>
    <ul class="pinned-posts-wrapper">
        {% for post in collections.blog %}
        {% if post.data.pinned %}
        <li style="list-style-type:none;" class="pinned-post-wrapper">
            <a href="{{ post.url }}" class="pinned-post">
                <div class="pinned-post-title-wrapper">
                    <h4 style="margin:0;">{{ post.data.title }}</h4>
                    <small style="margin:0;">{{ post.date | date("MMMM d, yyyy") }}</small>
                </div>
            </a>
        </li>
        {% endif %}
        {% endfor %}
    </ul>
</div>
<hr />
<div class="wrapper" style="margin-top:0px;">
    <h4 style="width:100%;margin-top:0px;">All Posts</h4>
    <ul class="posts-wrapper">
        {% for post in collections.blog %}
        <li style="list-style-type:none;" class="post-wrapper">
            <a href="{{ post.url }}" class="post">
                <div class="post-title-wrapper">
                    <h4 style="margin:0;">{{ post.data.title }}</h4>
                    <small style="margin:0;">{{ post.date | date("MMMM d, yyyy") }}</small>
                </div>
            </a>
        </li>
        {% endfor %}
    </ul>
</div>
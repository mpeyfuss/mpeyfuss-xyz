---
layout: layout.njk
title: Portfolio
---

<style>
    .wrapper {
        width: 100%;
        display: flex;
        justify-content: center;
        margin-top: 4rem;
    }

    .posts-wrapper {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        row-gap: 1rem;
        column-gap: 1rem;
        padding: 0;
        margin: 0;
    }

    .post-wrapper {
        display: flex;
        border: 1px solid rgb(63, 63, 70);
        border-radius: 0.5rem;
        list-style: none;
        width: 20rem;
        height: 20rem;
        transition-duration: 150ms;
        transition-timing-function: ease-in-out;
        margin: 0;
    }

    .post-wrapper:hover {
        background-color: var(--amber-500);
    }

    .post {
        display: flex;
        flex-direction: column;
        row-gap: 0.25rem;
        padding: 1rem 1rem;
        width: 100%;
    }

    @media (max-width: 1080px) {
        .posts-wrapper {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    @media (max-width: 768px) {
        .posts-wrapper {
            grid-template-columns: repeat(1, 1fr);
        }
    }
</style>

<h1 style="margin-bottom:0.5rem;">Portfolio</h1>
<h4 class="gradient-text" style="text-align:center;margin-top:0.5rem;">Meticulously executed projects
    with amazing
    creators
</h4>

<div class="wrapper">
    <ul class="posts-wrapper">
        {% for post in collections.portfolio %}
        <li style="list-style-type:none;" class="post-wrapper">
            <a href="{{ post.url }}" class="post">
                <h4 style="margin:0;">{{ post.data.title }}</h4>
                <p style="margin:0;font-weight:500;">{{ post.data.artist }}</p>
                <small style="margin:0;font-weight:300;">{{ post.date | date("MMMM d, yyyy") }}</small>
            </a>
        </li>
        {% endfor %}
    </ul>
</div>
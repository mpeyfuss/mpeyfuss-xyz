<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/png" href="/assets/favicon.png" />

  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ description }}">
  <meta property="og:image" content="{{ image or '/assets/og/default.png' }}">
  <meta property="og:type" content="article">
  <meta property="og:url" content="{{ page.url | url }}">

  <title>{{ title or "Home" }} // mpeyfuss.xyz</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap"
    rel="stylesheet">

  <style>
    :root {
      --zinc: #09090b;
      --red-500: #EF4444;
      --amber-500: #F59E0B;
      --teal-500: #14B8A6;
      --violet-500: #8B5CF6;
    }

    html,
    .grid-bg {
      background-color: #fafafa;
      background-image: radial-gradient(circle, #e4e4e7 1px, transparent 1px);
      background-size: 20px 20px;
    }

    body {
      font-family: 'Inter', sans-serif;
      /* background: #fafafa; */
      color: var(--zinc);
      margin: 0;
      padding: 0;
    }

    main {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .content-wrapper {
      width: 90%;
      max-width: 876px;
      margin-top: 4rem;
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 4rem;
      text-align: center;
    }

    h2 {
      font-size: 2.5rem;
    }

    h3 {
      font-size: 2rem;
    }

    h4 {
      font-size: 1.5rem;
    }

    h5 {
      font-size: 1.25rem;
      text-align: center;
      font-weight: 500;
      font-style: italic;
    }

    .gradient-text {
      font-weight: 600;
      background: linear-gradient(to right, var(--red-500), var(--amber-500), var(--teal-500), var(--violet-500));
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    p {
      font-size: 1rem;
      line-height: 150%;
    }

    a {
      font-size: 1rem;
      color: var(--amber-500);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
      text-underline-offset: 4px;
    }

    li {
      margin: 1rem 0rem;
    }

    img {
      max-width: 100%;
    }

    button {
      font-size: 1rem;
      border: 1px solid #09090b;
      border-radius: 0.5rem;
      padding: 0.5rem 1rem;
      background-color: #fafafa;
      cursor: pointer;
      transition-duration: 150ms;
      transition-timing-function: ease-in-out;
    }

    button:hover {
      scale: 102%;
    }

    .primary {
      background-color: var(--amber-500);
    }

    blockquote {
      font-style: italic;
      border-left: 4px solid #ccc;
      padding-left: 1rem;
      margin-left: 0;
      color: #555;
    }

    hr {
      border: 0.5px solid #d4d4d8;
      border-style: solid;
      margin: 1rem 0rem;
      width: 100%;
    }

    .nav {
      width: 100%;
      position: fixed;
      top: 0px;
      left: 0px;
      z-index: 999;
      background-color: rgba(250, 250, 250, 1);
      display: flex;
      justify-content: center;
    }

    .nav-wrapper {
      display: flex;
      justify-content: space-between;
      max-width: 1200px;
      width: 100%;
      padding: 0.75rem 1rem;
    }

    .pfp-header {
      width: 2rem;
      height: 2rem;
      border-radius: 100%;
    }

    .logo-text {
      font-size: 0.9rem;
    }

    .nav-logo {
      display: flex;
      column-gap: 0.5rem;
      align-items: center;
    }

    .nav-links {
      display: flex;
      column-gap: 1rem;
      align-items: center;
    }

    .nav-link {
      font-size: 0.9rem;
    }

    .social {
      display: flex;
      column-gap: 0.5rem;
      align-items: center;
      margin-bottom: 1rem;
    }

    @media (max-width: 768px) {

      .header-text {
        display: none;
      }

      .social {
        margin-bottom: 4rem;
      }
    }
  </style>
</head>

<body>
  <header class="nav grid-bg">
    <div class="nav-wrapper">
      <div class="nav-logo">
        <img src="/assets/pfp.png" class="pfp-header" alt="pfp header" />
        <a class="nav-link header-text" href="/">mpeyfuss.xyz</a>
      </div>
      <div class="nav-links">
        <a class="nav-link" style="color:var(--red-500);" href="/">Home</a>
        <!-- <a class="nav-link" style="color:var(--amber-500);" href="/portfolio">Portfolio</a> -->
        <a class="nav-link" style="color:var(--teal-500);" href="/tech">Tech</a>
        <a class="nav-link" style="color:var(--violet-500);" href="/blog">Blog</a>
      </div>
    </div>
  </header>

  <main>
    <div class="content-wrapper">
      {{ content | safe }}
    </div>
    <hr />
    <div class="social">
      <a href="https://x.com/mpeyfuss" target="_blank" rel="noreferrer noopener">Twitter</a>
      <p style="font-weight:600;margin:0;">//</p>
      <a href="https://github.com/mpeyfuss" target="_blank" rel="noreferrer noopener">Github</a>
      <p style="font-weight:600;margin:0;"> //</p>
      <a href="https://www.linkedin.com/in/marcopeyfuss/" target="_blank" rel="noreferrer noopener">LinkedIn</a>
    </div>
  </main>
</body>

</html>